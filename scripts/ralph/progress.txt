# Ralph Progress Log — CRM Fenice (Social Addiction Redesign)
Started: 2026-04-08

## Codebase Patterns
- Tailwind v4: custom theme in globals.css con design system premium (ash-*, ember-*, gold-*)
- Gamification engine: awardXpAndCoins(userId, actionType, leadIdContext) in src/lib/gamificationEngine.ts
- CelebrationOverlay componente gia' esiste in src/components/CelebrationOverlay.tsx — estenderlo
- AnimationToggle e getAnimationsEnabled() in src/lib/animationUtils.ts — rispettare il toggle
- RealtimeProvider in src/components/providers/RealtimeProvider.tsx — usare per eventi live
- Supabase Realtime canali: 'public:leads-changes' (non toccare), 'conferme_realtime_board'
- LeaderboardClient gia' fa polling 10s — estendere con animazioni
- StreakCounter componente in src/components/StreakCounter.tsx — gia' ha logica at-risk
- QuestPanel in src/components/QuestPanel.tsx — gia' mostra quest con progress
- Store items usano cssValue con prefissi: skin-avatar-*, skin-theme-*, skin-effect-*
- IMPORTANTE: NON fare deploy. Solo lavoro locale. NON pushare, NON mergiare.
- DATI REALI: NON toccare lead GDO 114, appuntamenti Conferme, account esistenti
- Per audio: usare Web Audio API per suoni sintetici leggeri, evitare file pesanti
- RewardFeedProvider in src/components/providers/RewardFeedProvider.tsx — global popup queue
- emitRewardEarned() in src/lib/animationUtils.ts — dispatch reward_earned CustomEvent
- awardXpAndCoins returns RewardData | null — server actions pass rewardData in response
- Server actions return { success: true, rewardData } — client code calls emitRewardEarned after success
- Daily idempotency: check coinTransactions with reason + gte(createdAt, todayStart) — no schema change needed
- DailyLoginReward pattern: localStorage quick rejection + server-side idempotent claim
- ActivityFeed pattern: aggregate multiple tables independently in server action, merge+sort in JS, return top N
- Feed refresh: setInterval(30s) + window.addEventListener('reward_earned') for immediate updates
- SocialNotificationProvider: 3 Supabase Realtime channels (social-achievements, social-bossbattles, social-seasonal) + custom event 'social_notification'
- emitSocialNotification() in animationUtils.ts for programmatic social toasts
- Toast types config: per-type icon, borderColor, iconColor, glowColor in SocialNotificationToast.tsx
- QuestChainModal in src/components/QuestChainModal.tsx — shows quest completion celebration + next quest teaser
- Quest chain pattern: after completeQuest, reload getUserQuests(), find next uncompleted, show chain modal
- StreakAnxietyBanner: time-based state (hidden/at-risk/critical/safe) using Intl.DateTimeFormat for Rome timezone
- StreakCounter visual states: safe (emerald), at-risk (red), default (streak-tier colors) — driven by isActiveToday + getRomeHour()
- Time-based UI: use setInterval(60000) for minute-by-minute state transitions, setInterval(1000) for countdown timers

---

## 2026-04-08 - SA-001
- Implemented micro-reward popup system for every gamification action
- Created RewardPopup component with slide-up animation, glow effect, CSS particles
- Created RewardFeedProvider context/provider for global popup queue (max 3 visible)
- Modified awardXpAndCoins to return RewardData (xpGained, coinsGained, actionType, didLevelUp)
- Added emitRewardEarned() utility to animationUtils.ts
- Integrated reward emission in: pipelineActions (FISSATO), confermeActions (CONFERMATO, CHIUSO, PRESENZIATO), venditoreActions (DEAL_CHIUSO)
- Integrated client-side emission in: OutcomeModal, GdoQuickActions, ConfermeDrawer, VenditoreDrawer
- Added FISSATO award to pipelineActions when GDO sets appointment (was imported but unused)
- Added CSS keyframes: reward-glow-pulse, reward-particle-float + utility classes
- Mounted RewardFeedProvider in dashboard layout wrapping all children
- Variable reward messaging with 12 motivational messages (gamification-loops pattern)
- File modificati:
  - src/lib/gamificationEngine.ts (return RewardData)
  - src/lib/animationUtils.ts (emitRewardEarned)
  - src/components/RewardPopup.tsx (NEW)
  - src/components/providers/RewardFeedProvider.tsx (NEW)
  - src/app/(dashboard)/layout.tsx (add provider)
  - src/app/actions/pipelineActions.ts (FISSATO award + rewardData)
  - src/app/actions/confermeActions.ts (rewardData return)
  - src/app/actions/venditoreActions.ts (rewardData return)
  - src/components/OutcomeModal.tsx (emit reward)
  - src/components/GdoQuickActions.tsx (emit reward)
  - src/components/ConfermeDrawer.tsx (emit reward)
  - src/components/VenditoreDrawer.tsx (emit reward)
  - src/app/globals.css (reward popup keyframes)
  - scripts/ralph/prd.json (passes: true)
- **Learnings per iterazioni future:**
  - awardXpAndCoins era importata ma non usata in pipelineActions — ora awarda FISSATO
  - Pattern di integrazione: server action returns rewardData, client emits CustomEvent — riusabile per tutte le future story
  - Popup queue con MAX_VISIBLE=3 previene overlap di troppi popup
  - CSS-only particles: usa animation con staggered delays per effetto naturale
---

## 2026-04-08 - SA-002
- Implemented live activity feed in GDO dashboard showing recent team actions
- Created server action getActivityFeed() aggregating from leadEvents, userAchievements, questProgress, lootDrops
- Each source query limited to 10-20 results, merged and sorted desc, top 50 returned
- Created ActivityFeed client component with scrollable card list, color-coded borders by event type
- Features: user avatars/initials, relative timestamps (Italian), hover effects, slide-in animation for new items
- Auto-refresh every 30 seconds via setInterval
- Listens to 'reward_earned' custom event for immediate refresh on gamification actions
- Skeleton loading state with pulse animation
- Positioned in dashboard between QuestPanel and PipelineBoard
- Dynamically imported to avoid SSR issues
- File modificati:
  - src/app/actions/activityFeedActions.ts (NEW - server action aggregating 4 tables)
  - src/components/ActivityFeed.tsx (NEW - client component with feed UI)
  - src/app/(dashboard)/page.tsx (added ActivityFeed dynamic import + render)
  - scripts/ralph/prd.json (passes: true)
- **Learnings per iterazioni future:**
  - Pattern: aggregate multiple tables independently, merge in JS — avoids complex SQL unions
  - Auto-refresh + event-driven refresh pattern: setInterval(30s) + window.addEventListener('reward_earned')
  - Color system: orange (appointments), ember (quests), gold (achievements/legendary), ash (generic events)
  - Scrollable feed with max-h-[400px] keeps dashboard compact
---

## 2026-04-08 - SA-003
- Implemented daily login reward welcome screen with streak-based bonus coins
- Created server action checkDailyLoginStatus + claimDailyLogin (idempotent via coinTransactions check)
- Created DailyLoginReward modal component with 3 phases: welcome, claiming, claimed
- Bonus tiers: +5 base, +10 if streak >= 3, +20 if streak >= 7
- Coin cascade animation with emoji coins falling from top (20 particles, staggered)
- Animated flame icon with pulse/flicker based on streak tier (base/fiamma/inferno)
- localStorage check + server timestamp for once-per-day display
- Modal shows streak info, motivational messages, bonus tier breakdown
- CSS keyframes: coin-cascade, daily-flame-pulse, daily-flame-flicker, daily-coin-bounce, daily-count-up
- Dynamically imported in dashboard page (no ssr: false needed — client component handles it)
- File modificati:
  - src/app/actions/dailyLoginActions.ts (NEW - checkDailyLoginStatus + claimDailyLogin)
  - src/components/DailyLoginReward.tsx (NEW - modal component with 3 phases)
  - src/app/(dashboard)/page.tsx (added DailyLoginReward dynamic import + render)
  - src/app/globals.css (coin cascade + daily login animation keyframes + CSS vars)
  - scripts/ralph/prd.json (passes: true)
- **Learnings per iterazioni future:**
  - No schema migration needed: use coinTransactions reason='Daily login bonus' + gte(createdAt, todayStart) for idempotency
  - ssr: false in dynamic() not allowed in Server Components (Next.js 16) — client components handle their own SSR guards
  - Coin cascade: emoji-based particles are lightweight and visually effective
  - Streak tier calculation reused from streakUtils but inline to avoid circular deps
  - Pattern: localStorage for quick UI rejection, server for idempotent coin granting
---

## 2026-04-08 - SA-004
- Implemented push-style social notifications for important gamification events
- Created SocialNotificationToast component with slide-in-from-right animation, contextual icons, CTA buttons
- Created SocialNotificationProvider with Supabase Realtime integration on 3 channels:
  - social-achievements: INSERT on userAchievements → "X ha sbloccato un badge!" (skips self)
  - social-bossbattles: UPDATE on bossBattles → "Boss sconfitto!" on defeat
  - social-seasonal: INSERT on seasonalEvents → "Evento stagionale iniziato!" with multiplier info
- Added periodic boss battle timer check (every 60s): warns "Boss Battle: X ore rimaste!" when < 2 hours left
- Added emitSocialNotification() utility in animationUtils.ts for programmatic notifications
- Listens to custom event 'social_notification' for in-app triggers (e.g., rank changes from leaderboard)
- 4 notification types: rank_overtaken (ember), rare_achievement (gold), boss_battle_ending (ember-dark), seasonal_event (brand-orange)
- Max 3 toasts visible, auto-dismiss after 5 seconds, click to dismiss
- CSS: social-toast-glow (pulsing shadow), social-toast-shimmer (top accent line), social-toast-icon (pulse once)
- Respects getAnimationsEnabled() toggle
- Mounted SocialNotificationProvider in dashboard layout between RewardFeedProvider and SidebarProvider
- File modificati:
  - src/lib/animationUtils.ts (emitSocialNotification + types)
  - src/components/SocialNotificationToast.tsx (NEW)
  - src/components/providers/SocialNotificationProvider.tsx (NEW)
  - src/app/(dashboard)/layout.tsx (add provider)
  - src/app/globals.css (social toast animation keyframes + theme vars)
  - scripts/ralph/prd.json (passes: true)
- **Learnings per iterazioni future:**
  - Supabase Realtime: create separate channels per table (social-achievements, social-bossbattles, social-seasonal) — don't touch existing 'public:leads-changes' channel
  - Toast positioning: use top offset (not bottom like RewardPopup) to avoid overlap with reward popups
  - Boss battle timer: use useRef Set to track notified battle IDs, prevent duplicate notifications
  - Pattern: emitSocialNotification() can be called from leaderboard component when rank changes detected
  - CSS custom properties (--toast-glow-color) via inline style for per-type glow coloring in animations
---

## 2026-04-08 - SA-005
- Implemented quest chain modal: "just one more" effect when claiming completed quests
- Created QuestChainModal component with confetti particles, XP/coins animated counters, next quest teaser
- When claiming a quest in QuestPanel, modal shows: celebration + rewards, then slides in next uncompleted quest as teaser
- If no more quests available, shows "Hai completato tutto! Torna domani per nuove sfide"
- Confetti particles (35 CSS-only) with staggered timing and multi-color palette
- XP counter animates from 0 to value with increment timer (count-up effect)
- Trophy icon with gold glow pulse animation
- Next quest card shows: title, description, progress bar at 0%, reward preview (XP + coins)
- CTA button "Accetta la sfida!" / "Continua" depending on whether next quest exists
- Modal appears with scale+translateY spring entrance animation
- Backdrop with blur + click-to-dismiss (only after teaser appears)
- All animations respect getAnimationsEnabled() toggle
- CSS keyframes: quest-chain-confetti, quest-chain-modal-enter, quest-chain-glow, quest-chain-trophy-pulse, quest-chain-shimmer
- File modificati:
  - src/components/QuestChainModal.tsx (NEW — modal with celebration + next quest teaser)
  - src/components/QuestPanel.tsx (import QuestChainModal, show after claim, find next uncompleted quest)
  - src/app/globals.css (quest chain animation keyframes)
  - scripts/ralph/prd.json (passes: true)
- **Learnings per iterazioni future:**
  - Pattern: after completeQuest, reload quests data, then find next uncompleted from fresh data
  - QuestPanel has two components (QuestCard + QuestPanel) with similar JSX closing patterns — use unique context for edits
  - Confetti in modal: use inline styles with Math.random() for natural staggering, CSS keyframes for animation
  - XP count-up: simple setInterval with increment = value/30, clearing when target reached
  - Modal phases: 'celebrate' (1.5s) → 'teaser' (interactive) — progressive disclosure keeps attention
---

## 2026-04-08 - SA-006
- Implemented streak anxiety system: visual warnings and countdown when streak is about to break
- Created StreakAnxietyBanner component with 4 states: hidden, at-risk (18:00+), critical (20:00+), safe
- At-risk state: red pulsing banner with trembling flame icon, warning message, countdown to midnight (HH:MM:SS)
- Critical state (20:00+): intensified shake animation, more urgent red glow, stronger warning message
- Safe state: green compact badge "Streak sicura per oggi" with shield icon
- Live countdown timer updates every second using Intl.DateTimeFormat for Europe/Rome timezone
- Enhanced StreakCounter with visual state-based styling: emerald (safe), red (at-risk), default (tier-based)
- StreakCounter flame color, border, background gradient, glow animation all change based on visual state
- Status text under streak count changes: "Streak sicura oggi" / "Streak a rischio!" / "Completa una quest!"
- Banner listens to 'reward_earned' event to refresh streak data immediately when a quest is completed
- State re-checks every 60 seconds to handle time transitions (17:59 → 18:00, 19:59 → 20:00)
- All animations respect getAnimationsEnabled() toggle
- CSS keyframes: streak-anxiety-pulse, streak-anxiety-critical, streak-safe-glow, streak-flame-shake, streak-flame-shake-intense
- Integrated in all 3 dashboard pages: GDO, Conferme, Venditore
- File modificati:
  - src/components/StreakAnxietyBanner.tsx (NEW — time-based warning banner with countdown)
  - src/components/StreakCounter.tsx (enhanced with visual state system: safe/at-risk/default)
  - src/app/(dashboard)/page.tsx (added StreakAnxietyBanner dynamic import + render)
  - src/app/(dashboard)/conferme/page.tsx (added StreakAnxietyBanner dynamic import + render)
  - src/app/(dashboard)/venditore/page.tsx (added StreakAnxietyBanner dynamic import + render)
  - src/app/globals.css (5 new streak anxiety animation keyframes + declarations)
  - scripts/ralph/prd.json (passes: true)
- **Learnings per iterazioni future:**
  - Intl.DateTimeFormat with formatToParts() is the cleanest way to get individual time components in a specific timezone
  - Time-based UI state: use setInterval(60000) for state transitions, setInterval(1000) only for visible countdowns
  - StreakCounter visual state computed inline with IIFE for clean ternary logic
  - Banner refreshes on reward_earned event — immediate feedback when user completes a quest (banner switches to safe)
  - Multiple dashboard pages (GDO, Conferme, Venditore) all share StreakCounter and StreakAnxietyBanner
---
