# Ralph Progress Log — CRM Fenice (Fase 2)
Started: 2026-04-07

## Codebase Patterns
- Optimistic locking: ogni UPDATE su leads DEVE includere `eq(leads.version, lead.version)` nel WHERE + version increment + check `.returning()` length
- Timezone: usare `toLocaleDateString('en-CA', { timeZone: 'Europe/Rome' })` per date stringa, mai `toISOString().split('T')[0]`
- Timezone client: `toLocaleString('it-IT', { timeZone: 'Europe/Rome' })` per display
- Google Calendar: factory `createOAuth2Client()` per ogni operazione, mai singleton
- Server Actions ritornano `{ success: boolean, error?: string }`
- Tailwind v4: custom theme via @theme in globals.css, colori brand: brand-orange (#FFBE82), brand-charcoal (#272626)
- Gamification XP formula: `100 * (Level ^ 1.5)` per target XP, 5 stadi evoluzione Fenice
- `assignedToId` e' immutabile dopo import CSV — mai modificarlo
- DATI REALI: NON toccare lead GDO 114, appuntamenti Conferme, account esistenti

- ConfermeDrawer salesperson init: use `salespersonUserId` (UUID) NOT `salespersonAssigned` (display name) to prevent re-save corruption
- FreeBusy check: always run BEFORE db.update(), never after — returning success:false after commit causes phantom state
- UI modals: use `modal-backdrop` + `modal-content` classes, add ESC handler via `useCallback` + `useEffect`
- UI drawers: use `drawer-overlay` + `drawer-panel` + `drawer-header` (sticky glass blur) classes
- Form inputs: use `input-fenice` class (brand-orange focus ring), extend with `!border-[color]` for contextual borders
- Loading states: use `skeleton-line` / `skeleton-circle` / `skeleton-card` shimmer classes, not `animate-spin` spinners

---

## 2026-04-07 - F2-001
- **Bug**: Confirmed appointments not visible in venditori dashboard
- **Root cause (DB audit)**: ALL confirmed leads had `appointmentDate = null` (rescheduled/test data). The `isNotNull(appointmentDate)` filter in `getVenditoreAppointments` excluded them all.
- **Secondary bug**: ConfermeDrawer initialized `salesperson` from `lead.salespersonAssigned` (display name) instead of `lead.salespersonUserId` (UUID), causing data corruption on re-saves
- **Third bug**: `setConfermeOutcome` ran FreeBusy check AFTER DB update — if check failed, DB was committed but function returned `success: false`
- **Files modified:**
  - `src/app/actions/venditoreActions.ts` — removed `isNotNull(appointmentDate)` filter
  - `src/components/ConfermeDrawer.tsx` — init salesperson from `salespersonUserId`
  - `src/app/actions/confermeActions.ts` — moved FreeBusy check before DB update
- **Learnings per iterazioni future:**
  - Always check actual DB data via Supabase MCP before assuming code-level bugs
  - `salespersonUserId` is only set for confirmed leads, so it's sufficient as the sole filter
  - Never perform side-effect checks (calendar, external APIs) after DB commits
---

## 2026-04-07 - F2-002
- **Feature**: Target bonus venditori per fatturato
- **What was done:**
  - Added `salesTargetEur` (real, nullable) to `users` table in schema.ts
  - Applied ADDITIVE migration via Supabase MCP (`ALTER TABLE users ADD COLUMN IF NOT EXISTS`)
  - Created `updateVenditoreSalesTarget` and `getVenditoriWithTargets` server actions in managerRpgActions.ts
  - Added "Target Fatturato Venditori" section in manager-targets page with inline editing per venditore
- **Files modified:**
  - `src/db/schema.ts` — added salesTargetEur field
  - `src/app/actions/managerRpgActions.ts` — added updateVenditoreSalesTarget + getVenditoriWithTargets
  - `src/app/(dashboard)/manager-targets/page.tsx` — fetch venditori data in server component
  - `src/app/(dashboard)/manager-targets/ManagerTargetsClient.tsx` — venditori target table with inline edit UI
- **Learnings per iterazioni future:**
  - Follow `updateGdoBaseSalary` pattern for simple user field updates (no optimistic locking needed for user config fields)
  - `getVenditoriWithTargets` filters active venditori only — consistent with GDO patterns
  - Always use `IF NOT EXISTS` in DDL migrations for idempotency
---

## 2026-04-07 - F2-003
- **Feature**: KPI Venditori — classifica con obiettivo bonus
- **What was done:**
  - Added `salesTargetEur` to venditori query in `getVenditoriKpi` server action
  - Added "Target" column showing the target amount (or dash if not set)
  - Added "Progresso" column with colored progress bar (green >=100%, yellow >=70%, red otherwise)
  - Added "BONUS RAGGIUNTO" badge with Award icon when fatturato >= salesTargetEur
  - Updated colspan for empty state row
  - Sorting remains by fatturato descending (unchanged)
- **Files modified:**
  - `src/app/actions/kpiVenditoriActions.ts` — added salesTargetEur to select and return
  - `src/components/KpiVenditoriClient.tsx` — added Target + Progresso columns with progress bar and bonus badge
- **Learnings per iterazioni future:**
  - `salesTargetEur` can be null (no target set) — handle gracefully with dash placeholder
  - Progress percentage uses IIFE pattern inside JSX for complex inline logic with multiple variables
  - Bar width capped at 100% visually even if percentage exceeds 100%
---

## 2026-04-07 - F2-004
- **Feature**: Setup 5 account venditori funzionanti
- **What was done:**
  - Rewrote `scripts/seedVenditori.ts` to follow the established dual-creation pattern (Supabase Auth + public.users)
  - Creates 5 venditori: sales001-005@fenice.local with password Venditore2026!
  - Each account created in Supabase Auth first (with email_confirm: true), then in public.users with matching UUID
  - Idempotent: checks if DB record exists before creating; handles "already registered" Auth errors
  - Generates formatted report table with email, name, status, and user ID
- **Files modified:**
  - `scripts/seedVenditori.ts` — full rewrite following verifyGdoAccounts.ts pattern
- **Learnings per iterazioni future:**
  - Users must exist in BOTH Supabase Auth and public.users with the same UUID to log in
  - The `verifyGdoAccounts.ts` pattern is the canonical way to create accounts: Auth first → get UUID → insert DB
  - Old seedVenditori.ts used random UUIDs without Auth creation — those accounts could never log in
  - Always use `email_confirm: true` in auth.admin.createUser to skip email verification
---

## 2026-04-07 - F2-005
- **Feature**: Scheda lead editabile dal ContactDrawer
- **What was done:**
  - Added `version` field to `getLeadProfile` return (needed for optimistic locking in the drawer)
  - Created `updateLeadContactInfo` server action in `eventActions.ts` with full optimistic locking (version check + increment)
  - Modified ContactDrawer to support inline edit mode for name, phone, email, and note (lastCallNote)
  - Added "Modifica" button to toggle edit mode, "Salva"/"Annulla" buttons in edit mode
  - Visual feedback: green success banner, red error banner, loading spinner on save
  - Handles CONCURRENCY_ERROR with auto-refresh, unique phone constraint error (23505), and general errors
  - Replaced `<span>` tags with `<div>` in timeline section to prevent hydration issues (CLAUDE.md rule)
  - Replaced `<p>` with `<div>` in quick actions description for hydration safety
  - Added `contact_info_edited` event type to timeline with proper label
- **Files modified:**
  - `src/app/actions/eventActions.ts` — added version to getLeadProfile, added updateLeadContactInfo action
  - `src/components/ContactDrawer.tsx` — full edit mode with inline fields, save/cancel, feedback UI
- **Learnings per iterazioni future:**
  - `getLeadProfile` didn't return `version` — always check what fields the profile query returns before building edit UIs
  - confermeActions uses direct `db.insert(leadEvents)` with custom event types (not the typed `logLeadEvent` helper) — follow same pattern for custom events
  - Phone field has UNIQUE constraint — handle postgres error code 23505 for duplicate phone
  - `lastCallNote` is the best "general note" field on leads for ContactDrawer context
---

## 2026-04-07 - F2-006
- **Feature**: Giorni lavorativi mensili manuali
- **What was done:**
  - Added `workingDaysOverride` (integer, nullable) to `monthlyTargets` table in schema.ts
  - Applied ADDITIVE migration via Supabase MCP (`ALTER TABLE ADD COLUMN IF NOT EXISTS`)
  - Updated `MonthlyTargetInput` and `TargetStatsResponse` interfaces with the new field
  - In `getManagerTargetsData`: if `workingDaysOverride` is set and > 0, it replaces auto-calculated `giorniLavorativiTotaliMese`; elapsed days capped at `min(calculated, override)`
  - In `confermeKpiActions.ts`: fetches `monthlyTargets` for current month and applies override to `totalWorkingDays` and `workingDaysPassed`
  - UI: added "Giorni Lav." input in "Imposta Target" dialog with "Reset Auto" button to clear override
  - Stat card "Giorni Lavorativi Mese" shows "Override manuale" label in orange when override is active
- **Files modified:**
  - `src/db/schema.ts` — added workingDaysOverride field
  - `src/app/actions/targetActions.ts` — interfaces + override logic in getManagerTargetsData
  - `src/app/actions/confermeKpiActions.ts` — import monthlyTargets, fetch override, apply to working days
  - `src/app/(dashboard)/manager-targets/ManagerTargetsClient.tsx` — form field + stat card indicator
- **Learnings per iterazioni future:**
  - `saveMonthlyTarget` uses `...target` spread — new fields in `MonthlyTargetInput` are automatically persisted
  - targetActions (Mon-Sat) and confermeKpiActions (Mon-Fri) have different working day definitions — override replaces both uniformly
  - `giorniLavorativiTrascorsiOggi` must be capped at override value to avoid elapsed > total
  - Use `placeholder="Auto"` for nullable numeric inputs to signal the default behavior
---

## 2026-04-07 - F2-007
- **Feature**: Storico pause mensile visibile ai GDO
- **What was done:**
  - Created `getGdoPauseHistory` server action in `pauseActions.ts` — fetches all breakSessions for current month filtered by gdoUserId, with live sync for in_corso sessions
  - Created `GdoPauseHistory` client component with monthly summary cards (total pauses, total duration, exceeded count, exceeded time) and detailed table (date, start time, duration, status, exceeded time)
  - Created `/storico-pause` page restricted to GDO role only
  - Added "Storico Pause" link with Clock icon in GDO sidebar navigation
- **Files modified:**
  - `src/app/actions/pauseActions.ts` — added PauseHistoryEntry type + getGdoPauseHistory action
  - `src/components/GdoPauseHistory.tsx` — new client component
  - `src/app/(dashboard)/storico-pause/page.tsx` — new page
  - `src/components/Sidebar.tsx` — added GDO sidebar link
- **Learnings per iterazioni future:**
  - `breakSessions.dateLocal` is stored as "YYYY-MM-DD" string — use SQL LIKE for month filtering
  - Follow `getManagerPauseReport` pattern for live in_corso sync in read-only queries
  - GDO pages use same auth pattern: createClient → getUser → check role → redirect if unauthorized
---

## 2026-04-07 - F2-008
- **Feature**: Form aggiunta lead singolo manuale
- **What was done:**
  - Created `createManualLead` server action in `importLeads.ts` — validates phone (5+ digits), optional email, required funnel, deduplication check, assigns via stored assignment settings (round-robin/custom quota)
  - Created `AddLeadModal.tsx` component — modal with form fields (nome, telefono, email, funnel), client-side + server-side validation, success/error feedback
  - Added "Aggiungi Lead Singolo" button on the import page header, opening the modal
  - Lead created with status=NEW, callCount=0, logs IMPORTED + ASSIGNED events with metadata `source: 'manual'`
- **Files modified:**
  - `src/app/actions/importLeads.ts` — added ManualLeadInput type + createManualLead server action
  - `src/components/AddLeadModal.tsx` — new modal component
  - `src/app/(dashboard)/import/page.tsx` — added button + modal integration
- **Learnings per iterazioni future:**
  - `previewLeadDistribution(1, ...)` with round-robin assigns to the first active GDO — works correctly for single lead
  - Reuse same validation logic (phone cleaning, email format check) as processCsvImport for consistency
  - Modal pattern: fixed inset-0 z-50, backdrop with onClick close, form with onSubmit
---

## 2026-04-07 - F2-009
- **Feature**: Note GDO: sistema HR/formazione (schema e backend)
- **What was done:**
  - Created `gdoNotes` table in schema.ts: id, gdoUserId, authorUserId, content, category, createdAt
  - Applied ADDITIVE migration via Supabase MCP (`CREATE TABLE IF NOT EXISTS`)
  - Created `gdoNotesActions.ts` with three server actions:
    - `createGdoNote`: validates content + category, inserts with randomUUID
    - `getGdoNotes(gdoUserId)`: returns notes with author name via LEFT JOIN on users, ordered by createdAt desc
    - `deleteGdoNote(noteId, authorUserId)`: deletes only if author matches (authorization check)
  - Exported `GdoNote` interface and `GdoNoteCategory` type for F2-010 UI consumption
- **Files modified:**
  - `src/db/schema.ts` — added gdoNotes table
  - `src/app/actions/gdoNotesActions.ts` — new file with 3 server actions
- **Learnings per iterazioni future:**
  - `gdoNotes` is separate from `confirmationsNotes` (lead notes) — different purpose and foreign keys
  - Delete uses AND condition (noteId + authorUserId) for authorization — only the author can delete their own notes
  - Category is a text field with app-level validation, not a DB constraint — allows flexibility
---

## 2026-04-07 - F2-010
- **Feature**: Note GDO: pagina UI dashboard Team Leader
- **What was done:**
  - Added `getGdoUsersForNotes` server action in `gdoNotesActions.ts` — fetches active GDO users with id, name, displayName, gdoCode, level
  - Created `NoteGdoClient.tsx` client component with:
    - Left panel: list of active GDO operators with name, code, and level
    - Right panel: notes for selected GDO with category badges, date, author, and delete button (author-only)
    - Add note form with category pill selector (formazione/positivo/negativo/disciplinare) and textarea
    - Category filter dropdown
    - Success/error feedback banners
  - Created `/note-gdo` page restricted to MANAGER and ADMIN roles (marketing excluded)
  - Added "Note GDO" link with FileText icon in Sidebar for MANAGER/ADMIN
- **Files modified:**
  - `src/app/actions/gdoNotesActions.ts` — added GdoUserForNotes interface + getGdoUsersForNotes action
  - `src/components/NoteGdoClient.tsx` — new client component
  - `src/app/(dashboard)/note-gdo/page.tsx` — new page
  - `src/components/Sidebar.tsx` — added sidebar link + FileText import
- **Learnings per iterazioni future:**
  - Reuse `getGdoNotes`/`createGdoNote`/`deleteGdoNote` from F2-009 — no new server actions needed for CRUD
  - Manager pages follow consistent auth pattern: createClient → getUser → check role → redirect marketing → render
  - Category filter with "all" default is a clean pattern for nullable filter state
---

## 2026-04-07 - F2-011
- **Feature**: Dashboard GDO: aggiungere chiusure, presenze e conferme
- **What was done:**
  - Added `getGdoLeadOutcomeMetrics(gdoUserId)` server action in `gdoPerformanceActions.ts` — fetches current month leads assigned to GDO and counts fissati, confermati, presenziati, chiusi using same logic as `getManagerGdoTables`
  - Created `GdoLeadMetrics.tsx` client component with 4 metric cards (Appuntamenti Fissati, Confermati, Presenze, Chiusure) — color-coded with icons, loading skeleton, and realtime_update listener
  - Integrated component at the top of the GDO dashboard page (above PipelineBoard)
- **Files modified:**
  - `src/app/actions/gdoPerformanceActions.ts` — added getGdoLeadOutcomeMetrics action
  - `src/components/GdoLeadMetrics.tsx` — new client component
  - `src/app/(dashboard)/page.tsx` — added GdoLeadMetrics import and placement
- **Learnings per iterazioni future:**
  - Reuse `isPresenziato()` helper from same file for consistent outcome classification
  - GDO dashboard page (`/`) reaches GDO users only — managers/conferme/venditori are redirected
  - `session!.user.id` is safe in the dashboard page because auth is checked before rendering
---

## 2026-04-07 - F2-012
- **Feature**: Obiettivi giornalieri sempre visibili
- **What was done:**
  - Created `getGdoDailyObjectives(gdoUserId)` server action in `gdoPerformanceActions.ts` — counts today's calls (from callLogs), today's appointments (from leads.appointmentCreatedAt), pipeline size, and reads dailyApptTarget
  - Created `getConfermeDailyObjectives(confermeUserId)` server action in `confermeKpiActions.ts` — counts today's confirmations and derives daily target from confermeTargetTier1 / 5 (Mon-Fri working days)
  - Created `GdoDailyObjectives.tsx` — dark gradient banner with 2 progress cards: "Chiamate oggi" (X/pipeline) and "Fissaggi oggi" (X/dailyApptTarget) with colored progress bars and completion indicator
  - Created `ConfermeDailyObjectives.tsx` — blue gradient banner with 1 progress card: "Confermati oggi" (X/dailyTarget) with progress bar and percentage
  - Integrated GdoDailyObjectives at top of GDO dashboard (above GdoLeadMetrics)
  - Integrated ConfermeDailyObjectives in Conferme dashboard (only for CONFERME role, not MANAGER/ADMIN)
- **Files modified:**
  - `src/app/actions/gdoPerformanceActions.ts` — added getGdoDailyObjectives + imported callLogs, or
  - `src/app/actions/confermeKpiActions.ts` — added getConfermeDailyObjectives
  - `src/components/GdoDailyObjectives.tsx` — new client component
  - `src/components/ConfermeDailyObjectives.tsx` — new client component
  - `src/app/(dashboard)/page.tsx` — added GdoDailyObjectives import and placement
  - `src/app/(dashboard)/conferme/page.tsx` — added ConfermeDailyObjectives import and conditional placement
- **Learnings per iterazioni future:**
  - `callLogs.userId` tracks who made the call — use for counting today's calls per GDO
  - `leads.appointmentCreatedAt` timestamp marks when an appointment was set — use gte/lte for today range
  - Conferme daily target: `Math.ceil(confermeTargetTier1 / 5)` — tier1 is weekly, 5 working days Mon-Fri
  - Conferme objectives only visible to CONFERME role, not to MANAGER/ADMIN who also access /conferme
  - Both components listen to `realtime_update` event for live refresh, following GdoLeadMetrics pattern
---

## 2026-04-07 - F2-013
- **Feature**: Coefficiente produttivita' GDO nei KPI
- **What was done:**
  - Added `productivityCoeff`, `contactedLeads`, and `callsPerHour` to `gdoStats` return in `getAdvancedKpi`
  - Formula: (calls / 6.5h) * (apptRate / 100) — measures effective appointments per hour worked
  - Added "Classifica Produttività GDO" ranking table in KpiGdoBoard with all GDO operators
  - Table shows: rank, name, calls, contacted leads, appointments, % fissaggio, calls/hour, coefficient
  - Sortable by coefficient (default), calls, appointments, or % fissaggio
  - Color-coded coefficient: green >=2, yellow >=1, red <1 with relative progress bar
  - Top-ranked GDO highlighted with orange accent
  - Updated "Chiamate/Ora" card from N/D to actual calculated value (totalCalls / 6.5)
- **Files modified:**
  - `src/app/actions/kpiAdvancedActions.ts` — added productivityCoeff, contactedLeads, callsPerHour to gdoStats
  - `src/components/KpiGdoBoard.tsx` — added ranking table + updated calls/hour card
- **Learnings per iterazioni future:**
  - `gdoStatsMap` already tracks `totalContacted` as a Set — just expose `.size` for the coefficient calculation
  - 6.5h is the fixed working hours constant (13:30-20:00) — defined as `WORKING_HOURS_PER_DAY` in server action
  - Coefficient is best understood as "effective appointments per hour" — combines volume and quality
  - For multi-day filters, the coefficient scales linearly with days but ranking remains valid for comparison
---

## 2026-04-07 - F2-014
- **Feature**: UI: Design system premium con Stitch
- **What was done:**
  - Used Stitch MCP to generate "CRM Fenice Premium" design system with Fidelity color variant
  - Extended @theme in globals.css with comprehensive design tokens while preserving all existing brand colors
  - **Color scales added**: brand-orange (50-900), ember (50-900, fire red), gold (50-900, prestige), ash (50-900, warm neutrals)
  - **Shadows**: soft, card, elevated, glow-orange, glow-ember, glow-gold
  - **Border radius**: sm (6px), md (8px), lg (12px), xl (16px), 2xl (24px)
  - **Animations**: fade-in, slide-up, slide-in-right, glow-pulse, shimmer
  - **Typography utilities**: display-lg/md/sm, heading-lg/md, body-lg/md/sm, caption, mono — display uses Sora font
  - **Component classes**: btn-primary/secondary/ghost/danger, input-default, badge/badge-orange/ember/gold/ash/success, card-default/elevated/warm, avatar sm/md/lg + orange/ember/gold, skeleton shimmer, glass effect, gradient backgrounds
  - All 280 existing brand color usages across 51 files remain untouched — fully backward compatible
- **Files modified:**
  - `src/app/globals.css` — extended @theme + added @layer utilities/components (422 lines added)
- **Learnings per iterazioni future:**
  - Tailwind v4 @theme block supports custom --shadow-*, --radius-*, --animate-* tokens natively
  - @layer components classes can be combined with Tailwind utilities freely
  - Stitch FIDELITY variant stays close to provided override colors — best for brand consistency
  - font-display registered as `"Sora", var(--font-geist-sans)` for graceful fallback
  - Existing components don't need migration — new tokens are opt-in for F2-015 through F2-020
---

## 2026-04-07 - F2-015
- **Feature**: UI: Redesign Sidebar e Topbar
- **What was done:**
  - Created `SidebarProvider` context for mobile sidebar toggle state sharing between Sidebar and Topbar
  - **Sidebar redesign:**
    - Dark gradient background: `bg-gradient-to-b from-ash-800 via-brand-charcoal to-ash-900`
    - Logo with gradient orange icon and `shadow-glow-orange` effect
    - Nav items: `group` hover with icon color transition to `brand-orange-300`, active state with inset left border (`shadow-[inset_3px_0_0_var(--color-brand-orange)]`) and `bg-brand-orange/15`
    - Gradient dividers between sections (`via-white/10`)
    - Sign out button with `hover:text-ember-300` danger hint
    - Badge uses `ember-500` with `shadow-glow-ember`
    - Mobile: fixed overlay with backdrop blur, slide-in/out with `transition-transform duration-300`, close on route change
  - **Topbar redesign:**
    - Glass-like header: `bg-white/80 backdrop-blur-md border-ash-200/60 shadow-soft`
    - Mobile hamburger button (`Menu` icon) toggles sidebar via context
    - Search input: `rounded-lg`, `focus:ring-2 focus:ring-brand-orange/30`, `bg-ash-50/50`
    - Search dropdown: `rounded-xl shadow-elevated animate-fade-in`, replaced `<span>` with `<div>` in results for hydration safety
    - Notification bell: `hover:bg-ash-100 rounded-lg` with `ember-500` badge, dropdown uses `rounded-xl shadow-elevated animate-fade-in`
    - Wallet coins: gold gradient (`from-gold-50 to-brand-orange-50`), `text-gold-700`, `border-gold-200`
    - Avatar: gradient (`from-brand-orange to-brand-orange-500`), `ring-2 ring-brand-orange/20`, green online status dot
    - Live toast: `border-gold-400`, `shadow-elevated`, `animate-slide-in-right`
    - Notification items use `<div>` instead of `<p>` for hydration safety
    - User name hidden on small screens (`hidden sm:block`)
  - Updated dashboard layout to wrap with `SidebarProvider`
- **Files modified:**
  - `src/components/providers/SidebarProvider.tsx` — new context provider
  - `src/components/Sidebar.tsx` — full redesign
  - `src/components/Topbar.tsx` — full redesign
  - `src/app/(dashboard)/layout.tsx` — added SidebarProvider wrapper
- **Learnings per iterazioni future:**
  - `fixed` + `lg:relative` pattern works for mobile overlay / desktop in-flow sidebar — fixed elements don't participate in flex layout
  - `shadow-[inset_3px_0_0_var(--color-brand-orange)]` creates a left accent bar without extra DOM elements
  - Sidebar close on `pathname` change prevents stale mobile overlay after navigation
  - All `<p>` and `<span>` containing interactive elements replaced with `<div>` to prevent hydration WSOD
  - `backdrop-blur-md` on header creates a glass effect that works with theme backgrounds
---

## 2026-04-07 - F2-016
- **Feature**: UI: Redesign Pipeline GDO
- **What was done:**
  - **PipelineBoard redesign:**
    - Premium tab bar: `bg-ash-100/80` container with sliding active indicator animated via `transition-all duration-300`
    - Tab badges color-coded per call stage: brand-orange (1st), gold (2nd), ember (3rd/4th)
    - Glass-like sticky header: `bg-white/90 backdrop-blur-sm border-ash-200/60`
    - Smooth tab transitions: opacity fade (150ms) on tab switch using `isTransitioning` state
    - Staggered card entry: `animate-fade-in` with `animationDelay` per card (max 300ms)
    - List area: subtle gradient `from-ash-50/50 to-white`
    - Empty state: icon-based (Inbox) with `ash-*` warm neutral palette
    - 4th call tab: Flame icon with ember accents
  - **LeadCard redesign:**
    - Premium card container: `rounded-xl shadow-soft hover:shadow-card hover:border-brand-orange/30 transition-all duration-200`
    - Status badges: dot indicator + border + gradient backgrounds using design token colors (blue/gold/ember/emerald/brand-orange)
    - Expired recall: gradient left accent via `before:` pseudo-element (`from-ember-400 to-ember-600`)
    - Activity info: icon boxes (`w-7 h-7 rounded-lg`) with label + date two-line layout
    - Copy buttons: `hover:bg-brand-orange-50 hover:text-brand-orange` with `rounded-md`
    - Warm neutral palette (`ash-*`) replacing all `gray-*` for text and borders
    - Hydration safety: all interactive elements use `<div>` containers, never `<span>` or `<p>`
  - ALL existing functionality preserved: quick actions, outcome modal, recall watcher, copy buttons, responsive columns
- **Files modified:**
  - `src/components/PipelineBoard.tsx` — full redesign with sliding tabs, transitions, staggered animations
  - `src/components/LeadCard.tsx` — full redesign with premium status badges, activity boxes, warm palette
- **Learnings per iterazioni future:**
  - Sliding tab indicator via `getBoundingClientRect()` + `ref` is more elegant than conditional CSS classes
  - `animationDelay` with `animationFillMode: 'backwards'` creates staggered list entrance
  - `before:` pseudo-element for left accent avoids extra DOM and works with `relative` parent
  - Status dot (`w-1.5 h-1.5 rounded-full`) inside badge gives at-a-glance color signal
---

## 2026-04-07 - F2-017
- **Feature**: UI: Redesign Dashboard Conferme
- **What was done:**
  - **ConfermeBoard.tsx redesign:**
    - Premium segmented control tabs with icons (Sun/Sunrise/Clock), backdrop-blur glass header, brand-orange active badges
    - Timeline hour buttons: gradient active state (brand-orange-50), warm ash palette replacing gray-*
    - NR filter pills: ash gradient active state, brand-orange hover accent
    - Lead list container: ash borders, staggered fade-in animation per card row
    - Snooze sidebar: warm gradient header (brand-orange-50 to gold-50) replacing purple
    - Table view: gradient table header, premium date presets with gradient buttons, ember-accented status badges
    - Table rows: brand-orange hover accent, gradient status badges (emerald/ember/blue), rounded-lg chevron button
    - Empty states: gradient background, rounded-2xl icon container, ash palette
    - Loading state: added text label under spinner
    - Content area: gradient background from-ash-50/50 to-white
  - **ConfermeBoardRow.tsx redesign:**
    - Premium card: rounded-xl, shadow-soft, hover:shadow-card with brand-orange border accent
    - State-dependent card backgrounds: amber gradient (locked), ember gradient (overdue snooze), white (default)
    - Status badges: gradient backgrounds (emerald for confermato, ember for scartato), animate-fade-in
    - NR attempt dots: brand-orange-500 + ember-400 for 3rd call
    - Action buttons: ash borders, shadow-soft, hover:shadow-card with brand-orange/blue/ember accents
    - Popovers: shadow-elevated, animate-fade-in, gradient CTA buttons, brand-orange/blue focus rings
    - Call badge: gradient from-brand-orange-50 to-gold-50
    - Snooze badge: gradient with brand-orange (normal) or ember (overdue) palette
    - Notes section: brand-orange gradient background, rounded-lg
    - All `<span>` replaced with `<div>` for hydration safety
  - **conferme/page.tsx:** Updated header to use ash-800 text and ash-500 subtitle with `<div>` for hydration safety
  - ALL existing functionality preserved: drawer, quick actions, NR tracking, snooze, recall, presence, realtime, table view
- **Files modified:**
  - `src/components/ConfermeBoard.tsx` — full redesign with premium warm palette
  - `src/components/ConfermeBoardRow.tsx` — full redesign with premium cards and micro-animations
  - `src/app/(dashboard)/conferme/page.tsx` — header styling update
- **Learnings per iterazioni future:**
  - Conferme board uses tab-based navigation (pomeriggio/mattina/richiami/table) not traditional kanban columns
  - `backdrop-blur-md` on sticky nav creates glass effect consistent with F2-015 Topbar
  - Staggered card animations (animationDelay + backwards fill) work well for list rendering
  - Snooze sidebar was previously purple — changed to warm brand-orange/gold for design system consistency
  - Table row hover uses `brand-orange-50/30` for subtle warm accent matching overall theme
---

## 2026-04-07 - F2-018
- **Feature**: UI: Redesign Dashboard Venditori e KPI pages
- **What was done:**
  - **VenditoreDashboardClient.tsx redesign:**
    - Toolbar: glass-like `bg-white/90 backdrop-blur-sm`, ash-* warm palette, `shadow-soft` cards
    - View toggle: `bg-ash-100/80` segmented control matching F2-015/016 pattern
    - Table: gradient header `from-ash-50 to-ash-100/50`, staggered row fade-in, `hover:bg-brand-orange-50/30`
    - Agenda cards: `shadow-soft hover:shadow-card hover:border-brand-orange/30`, staggered animation
    - Status badges: emerald for Chiuso, ash for Sparito, ember for Non Chiuso (replaced gray/orange)
    - Drawer: `backdrop-blur-sm`, `shadow-elevated`, `animate-slide-in-right`
    - All `<span>` in data cells replaced with `<div>` for hydration safety
  - **KpiVenditoriClient.tsx redesign:**
    - **Podium for top 3**: gold/ash/brand-orange gradient circles with Crown/Medal icons, animated `slide-up`
    - Trophy position indicators: gradient circles (gold-300→500, ash-300→400, brand-orange-300→500)
    - Progress bars: `transition-all duration-700` for smooth animation, emerald/gold/ember color coding
    - "TU" badge: brand-orange-100 with border, BONUS badge with emerald and animate-fade-in
    - Table rows: staggered fade-in, brand-orange-50/30 highlight for current user
  - **KpiGdoBoard.tsx redesign:**
    - Filter toolbar: glass-like, ash palette, rounded-lg inputs with `focus:ring-brand-orange/30`
    - KPI cards: icon boxes in `rounded-xl` containers, `hover:shadow-card` transition, grouped hover colors
    - Appuntamenti card: emerald gradient background (not just border)
    - Fissaggio card: `bg-gradient-to-br from-brand-charcoal to-ash-900`
    - Target widgets: left accent gradient bars, `rounded-xl` icon containers
    - Chart tooltip: `borderRadius: '12px'`, ash border
    - Pie chart: ember/gold/emerald colors replacing red/amber/green
    - Productivity ranking: `hover:bg-brand-orange-50/20`, progress bars with `duration-700`
  - **ManagerTargetsClient.tsx redesign:**
    - Table headers: `bg-gradient-to-r from-ash-800 to-brand-charcoal`
    - Section headers: `bg-gradient-to-r from-ash-50 to-ash-100/50`
    - Stat cards: `hover:shadow-card transition-all duration-200`, ash palette
    - Critical alert: ember gradient with rounded-xl
    - Modal: `backdrop-blur-sm`, `rounded-2xl`, `shadow-elevated`
    - Buttons: using `btn-primary`, `btn-ghost` design system classes
    - All table rows: `hover:bg-brand-orange-50/20 transition-colors duration-200`
  - **ManagerOperativaBoard.tsx redesign:**
    - Header: gradient background, ash-100/80 tab bar
    - Totals row: `bg-gradient-to-r from-brand-charcoal to-ash-800`
    - Data rows: `hover:bg-brand-orange-50/20 transition-colors duration-200`
    - Appointments column: emerald-50/30 background accent
  - **ManagerGdoClient.tsx redesign:**
    - GDO card headers: `bg-gradient-to-r from-brand-charcoal to-ash-800`, avatar with `shadow-glow-orange`
    - Sub-tables: `rounded-xl`, gradient section headers, `hover:bg-brand-orange-50/20`
    - Staggered card entry animation per GDO card
    - Presence highlights: brand-orange-600 on brand-orange-50/30
  - **ManagerRpgClient.tsx redesign:**
    - Table header: gradient from-brand-charcoal to-ash-800
    - XP bars: `bg-gradient-to-r from-brand-orange to-brand-orange-600` with `duration-700`
    - Coins: gold-600 text, gold button styling
    - Staggered row animations, `hover:bg-brand-orange-50/20`
    - Dialog: `shadow-elevated`, coins button gradient from-gold-400 to-gold-500
  - **Page headers updated:** venditore, kpi-gdo, kpi-venditori — ash-800 text, ash-500 subtitles, `<div>` for hydration safety
  - ALL existing functionality preserved across all pages
- **Files modified:**
  - `src/components/VenditoreDashboardClient.tsx` — full redesign
  - `src/components/KpiVenditoriClient.tsx` — full redesign with podium
  - `src/components/KpiGdoBoard.tsx` — full redesign
  - `src/app/(dashboard)/manager-targets/ManagerTargetsClient.tsx` — full redesign
  - `src/components/ManagerOperativaBoard.tsx` — full redesign
  - `src/app/(dashboard)/manager-gdo-performance/ManagerGdoClient.tsx` — full redesign
  - `src/app/(dashboard)/manager-rpg-monitor/ManagerRpgClient.tsx` — full redesign
  - `src/app/(dashboard)/venditore/page.tsx` — header styling
  - `src/app/(dashboard)/kpi-gdo/page.tsx` — header styling
  - `src/app/(dashboard)/kpi-venditori/page.tsx` — header styling
- **Learnings per iterazioni future:**
  - Podium pattern: flex items-end with different heights (h-28, h-20, h-16) creates visual hierarchy
  - `animationDelay` + `animationFillMode: 'backwards'` pattern works consistently across all redesigned components
  - `btn-primary` and `btn-ghost` from design system replace long inline button classes — keeps code cleaner
  - `hover:shadow-card` paired with `shadow-soft` base creates subtle depth on hover without being heavy
  - Table row `hover:bg-brand-orange-50/20` is the standard warm hover across all manager/KPI tables
  - Crown/Medal lucide icons work well for podium — Trophy for rankings
---

## 2026-04-07 - F2-019
- **Feature**: UI: Redesign componenti comuni (modali, drawer, form)
- **What was done:**
  - Added new CSS animations to globals.css: `modal-enter` (scale+fade spring curve), `modal-exit`, `drawer-slide` (spring easing), `backdrop-in` (fade)
  - Added reusable CSS component classes: `modal-backdrop`, `modal-content`, `drawer-overlay`, `drawer-panel`, `drawer-header`, `input-fenice` (unified form input with brand-orange focus ring)
  - Added skeleton shimmer helper classes: `skeleton-line`, `skeleton-circle`, `skeleton-card`
  - **AddLeadModal**: backdrop blur, spring modal animation, ESC key close, `input-fenice` inputs, `btn-primary`/`btn-ghost` buttons
  - **EditAppointmentModal**: backdrop blur, spring modal animation, ESC key close, `input-fenice` inputs, `btn-primary`/`btn-secondary` buttons
  - **OutcomeModal**: `drawer-overlay` + `drawer-panel` for consistent drawer animation, ESC key close, `input-fenice` inputs
  - **ContactDrawer**: `drawer-overlay` + `drawer-panel` animation, `drawer-header` sticky header with glass blur, skeleton shimmer loading (DrawerSkeleton component), ESC key close, `input-fenice` inputs
  - **ConfermeDrawer**: `drawer-overlay` + `drawer-panel` animation, `drawer-header` sticky header, skeleton shimmer for notes loading (ConfermeDrawerSkeleton), `input-fenice` inputs
  - **VenditoreDrawer**: `drawer-header` sticky header with glass blur, consistent close button, `input-fenice` inputs, `btn-primary`/`btn-secondary` footer buttons
  - **GdoQuickActions**: `input-fenice` for select/datetime/textarea inputs (consistent focus ring)
- **Files modified:**
  - `src/app/globals.css` — 4 new keyframes, 4 new animation tokens, 9 new component classes
  - `src/components/AddLeadModal.tsx` — full restyle
  - `src/components/EditAppointmentModal.tsx` — full restyle
  - `src/components/OutcomeModal.tsx` — full restyle
  - `src/components/ContactDrawer.tsx` — skeleton loading + restyle
  - `src/components/ConfermeDrawer.tsx` — skeleton loading + restyle
  - `src/components/VenditoreDrawer.tsx` — restyle
  - `src/components/GdoQuickActions.tsx` — input-fenice consistency
- **Learnings per iterazioni future:**
  - `modal-backdrop` / `drawer-overlay` / `drawer-panel` / `drawer-header` / `input-fenice` are the canonical reusable classes — use these for ALL new modals/drawers/forms
  - `skeleton-line`, `skeleton-circle`, `skeleton-card` provide shimmer loading — always prefer over `animate-spin` spinners
  - Spring easing `cubic-bezier(0.32, 0.72, 0, 1)` for drawers and `cubic-bezier(0.16, 1, 0.3, 1)` for modals give the best "snappy" feel
  - ESC key handler pattern: `useCallback` + `useEffect` with cleanup, guarded by `isOpen`
  - `!important` overrides (e.g. `!border-green-200`) are needed when extending `input-fenice` with contextual border colors
---
