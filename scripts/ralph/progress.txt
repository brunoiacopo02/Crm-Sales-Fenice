# Ralph Progress Log — Riorganizzazione UI
Started: 2026-04-10

## Codebase Patterns
- walletCoins e' il campo CORRETTO per coins. MAI usare coins (legacy).
- SafeWrapper: OGNI componente gamification deve essere wrappato.
- Pipeline: NON modificare la logica. La pipeline e SACRA.
- DB: porta 6543 (pooler) su serverless, porta 5432 locale.
- Hydration: bottoni MAI child di <span> o <p>.
- ProfileClient.tsx: componente principale del profilo RPG. E un "use client" component.
- profilo/page.tsx: server component che fetcha tutti i dati e li passa a ProfileClient.
- InventarioCreatureClient: accetta props {allCreatures, ownedCreatures, isTeam, userId}
- MappaAvventuraClient: accetta props {progress, bosses, isTeam, userId}
- TradingClient: accetta props dal server (creature, offerte, GDO attivi)
- I popup globali (DailyLoginReward, TimedChest, RewardPopup, CelebrationOverlay, FomoToast, UniverseToast) sono nel layout.tsx — NON toccarli.
- GdoDailyObjectives: componente che mostra chiamate/fissaggi del giorno con progress bar.
- ChestWidget: widget bauli azione. Va RIMOSSO dalla pipeline ma il backend resta.

## Completed Stories

### RO-001 — Pipeline: obiettivi giornalieri sticky in alto (2026-04-10)
- Wrapped GdoDailyObjectives in sticky container: `sticky top-0 z-10 bg-gray-50 shadow-sm`
- Used negative margins (-mx-3 sm:-mx-6) + compensating padding to extend bg full-width
- Component stays fixed at top when scrolling pipeline. Build green.

### RO-002 — Pipeline: rimuovere widget gamification dalla dashboard (2026-04-10)
- Removed ChestWidget, DuelWidget, QuestPanel imports and renders from page.tsx
- Also removed StreakCounter, SeasonalEventBanner, BossBattleBanner, LevelNudge, StreakAnxietyBanner
- Kept overlay popups: CelebrationOverlay, LootDropModal, TimedChest, DailyLoginReward
- Kept core: GdoDailyObjectives (sticky), GdoLeadMetrics, HotStreak+PipelineBoard
- Pipeline is now clean: objectives + metrics + pipeline board only. Build green.

### RO-003 — Sidebar: rimuovere pagine gamification separate (2026-04-10)
- Removed GDO sidebar entries: "Inventario Creature" (/creature-inventario), "Scambi Creature" (/trading), "Mappa Avventura" (/mappa-avventura)
- Removed CONFERME sidebar entries: "Inventario Team" (/creature-inventario), "Avventura Team" (/mappa-avventura)
- Kept: Il mio Profilo, Classifica, Fenice Store for both roles
- Pages remain accessible via direct URL — only sidebar links removed
- Cleaned up unused imports (Sparkles, ArrowLeftRight). Build green.

## Obiettivo Run
La pipeline deve essere PULITA — solo lead + obiettivi fissi.
Tutta la gamification converge nel Profilo RPG con tab.
Le pagine separate (/creature-inventario, /mappa-avventura, /trading) restano accessibili via URL ma spariscono dalla sidebar.
