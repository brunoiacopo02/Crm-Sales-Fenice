# Ralph Progress Log — CRM Fenice (Social Addiction Redesign)
Started: 2026-04-08

## Codebase Patterns
- Tailwind v4: custom theme in globals.css con design system premium (ash-*, ember-*, gold-*)
- Gamification engine: awardXpAndCoins(userId, actionType, leadIdContext) in src/lib/gamificationEngine.ts
- CelebrationOverlay componente gia' esiste in src/components/CelebrationOverlay.tsx — estenderlo
- AnimationToggle e getAnimationsEnabled() in src/lib/animationUtils.ts — rispettare il toggle
- RealtimeProvider in src/components/providers/RealtimeProvider.tsx — usare per eventi live
- Supabase Realtime canali: 'public:leads-changes' (non toccare), 'conferme_realtime_board'
- LeaderboardClient gia' fa polling 10s — estendere con animazioni
- StreakCounter componente in src/components/StreakCounter.tsx — gia' ha logica at-risk
- QuestPanel in src/components/QuestPanel.tsx — gia' mostra quest con progress
- Store items usano cssValue con prefissi: skin-avatar-*, skin-theme-*, skin-effect-*
- IMPORTANTE: NON fare deploy. Solo lavoro locale. NON pushare, NON mergiare.
- DATI REALI: NON toccare lead GDO 114, appuntamenti Conferme, account esistenti
- Per audio: usare Web Audio API per suoni sintetici leggeri, evitare file pesanti
- RewardFeedProvider in src/components/providers/RewardFeedProvider.tsx — global popup queue
- emitRewardEarned() in src/lib/animationUtils.ts — dispatch reward_earned CustomEvent
- awardXpAndCoins returns RewardData | null — server actions pass rewardData in response
- Server actions return { success: true, rewardData } — client code calls emitRewardEarned after success
- ActivityFeed pattern: aggregate multiple tables independently in server action, merge+sort in JS, return top N
- Feed refresh: setInterval(30s) + window.addEventListener('reward_earned') for immediate updates

---

## 2026-04-08 - SA-001
- Implemented micro-reward popup system for every gamification action
- Created RewardPopup component with slide-up animation, glow effect, CSS particles
- Created RewardFeedProvider context/provider for global popup queue (max 3 visible)
- Modified awardXpAndCoins to return RewardData (xpGained, coinsGained, actionType, didLevelUp)
- Added emitRewardEarned() utility to animationUtils.ts
- Integrated reward emission in: pipelineActions (FISSATO), confermeActions (CONFERMATO, CHIUSO, PRESENZIATO), venditoreActions (DEAL_CHIUSO)
- Integrated client-side emission in: OutcomeModal, GdoQuickActions, ConfermeDrawer, VenditoreDrawer
- Added FISSATO award to pipelineActions when GDO sets appointment (was imported but unused)
- Added CSS keyframes: reward-glow-pulse, reward-particle-float + utility classes
- Mounted RewardFeedProvider in dashboard layout wrapping all children
- Variable reward messaging with 12 motivational messages (gamification-loops pattern)
- File modificati:
  - src/lib/gamificationEngine.ts (return RewardData)
  - src/lib/animationUtils.ts (emitRewardEarned)
  - src/components/RewardPopup.tsx (NEW)
  - src/components/providers/RewardFeedProvider.tsx (NEW)
  - src/app/(dashboard)/layout.tsx (add provider)
  - src/app/actions/pipelineActions.ts (FISSATO award + rewardData)
  - src/app/actions/confermeActions.ts (rewardData return)
  - src/app/actions/venditoreActions.ts (rewardData return)
  - src/components/OutcomeModal.tsx (emit reward)
  - src/components/GdoQuickActions.tsx (emit reward)
  - src/components/ConfermeDrawer.tsx (emit reward)
  - src/components/VenditoreDrawer.tsx (emit reward)
  - src/app/globals.css (reward popup keyframes)
  - scripts/ralph/prd.json (passes: true)
- **Learnings per iterazioni future:**
  - awardXpAndCoins era importata ma non usata in pipelineActions — ora awarda FISSATO
  - Pattern di integrazione: server action returns rewardData, client emits CustomEvent — riusabile per tutte le future story
  - Popup queue con MAX_VISIBLE=3 previene overlap di troppi popup
  - CSS-only particles: usa animation con staggered delays per effetto naturale
---

## 2026-04-08 - SA-002
- Implemented live activity feed in GDO dashboard showing recent team actions
- Created server action getActivityFeed() aggregating from leadEvents, userAchievements, questProgress, lootDrops
- Each source query limited to 10-20 results, merged and sorted desc, top 50 returned
- Created ActivityFeed client component with scrollable card list, color-coded borders by event type
- Features: user avatars/initials, relative timestamps (Italian), hover effects, slide-in animation for new items
- Auto-refresh every 30 seconds via setInterval
- Listens to 'reward_earned' custom event for immediate refresh on gamification actions
- Skeleton loading state with pulse animation
- Positioned in dashboard between QuestPanel and PipelineBoard
- Dynamically imported to avoid SSR issues
- File modificati:
  - src/app/actions/activityFeedActions.ts (NEW - server action aggregating 4 tables)
  - src/components/ActivityFeed.tsx (NEW - client component with feed UI)
  - src/app/(dashboard)/page.tsx (added ActivityFeed dynamic import + render)
  - scripts/ralph/prd.json (passes: true)
- **Learnings per iterazioni future:**
  - Pattern: aggregate multiple tables independently, merge in JS — avoids complex SQL unions
  - Auto-refresh + event-driven refresh pattern: setInterval(30s) + window.addEventListener('reward_earned')
  - Color system: orange (appointments), ember (quests), gold (achievements/legendary), ash (generic events)
  - Scrollable feed with max-h-[400px] keeps dashboard compact
---
