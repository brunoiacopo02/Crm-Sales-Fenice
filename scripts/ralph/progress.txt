# Ralph Progress Log — CRM Fenice Addiction v2
Started: 2026-04-08

## Codebase Patterns
- walletCoins e' il campo CORRETTO per coins visibili e spendibili. MAI usare il campo coins (legacy).
- awardXpAndCoins in gamificationEngine.ts: aggiorna walletCoins, applica streak multiplier, logga coinTransactions
- completeQuest in questActions.ts: aggiorna walletCoins, marca currentValue=-1 dopo claim
- buyShopItem in shopActions.ts: legge e scala walletCoins
- getUserWalletCoins in sprintActions.ts: legge walletCoins (usato dal Topbar)
- SafeWrapper in src/components/SafeWrapper.tsx: error boundary — usare per OGNI componente gamification
- Pipeline logica in pipelineActions.ts getPipelineLeads(): NON TOCCARE la logica, solo restyling
- LeadCard in src/components/LeadCard.tsx: il componente card della pipeline
- Sidebar in src/components/Sidebar.tsx: gia' riorganizzata con gruppi per Manager
- Topbar in src/components/Topbar.tsx: mostra wallet coins, notifiche, avatar
- StreakCounter in src/components/StreakCounter.tsx: mostra streak con fiamma
- QuestPanel in src/components/QuestPanel.tsx: mostra quest attive
- Store in src/components/ShopClient.tsx: negozio con categorie e filtri
- Profilo in src/app/(dashboard)/profilo/ProfileClient.tsx: profilo RPG
- Classifica in src/app/(dashboard)/classifica/LeaderboardClient.tsx: leaderboard con polling 10s
- CelebrationOverlay in src/components/CelebrationOverlay.tsx: overlay celebrazioni
- DailyLoginReward in src/components/DailyLoginReward.tsx: bonus giornaliero
- LootDropModal in src/components/LootDropModal.tsx: apertura loot
- soundEngine in src/lib/soundEngine.ts: suoni sintetici Web Audio API
- animationUtils in src/lib/animationUtils.ts: utility animazioni + toggle
- Design system: globals.css con @theme, colori: brand-orange (#FFBE82), brand-charcoal (#272626), ash-*, ember-*, gold-*
- Supabase Realtime: canale 'public:leads-changes' in RealtimeProvider — NON creare nuovi canali
- Connessione DB: diretta IPv4 (Supabase Pro + IPv4 add-on)
- Migrazioni: ESEGUIRE sul DB prod, non solo creare file. Bypass middleware se serve.

---

## FA-001 — Audit e fix completo sistema XP/coins/store end-to-end
**Status:** PASSED ✓
**Date:** 2026-04-08
**Commit:** 47deaf7

### Audit Results:
- gamificationEngine.ts awardXpAndCoins: ✓ uses walletCoins correctly
- questActions.ts completeQuest: ✓ uses walletCoins correctly
- shopActions.ts buyShopItem: ✓ uses walletCoins correctly
- sprintActions.ts getUserWalletCoins: ✓ uses walletCoins correctly
- Streak multiplier: ✓ applied in awardXpAndCoins + completeQuest
- coinTransactions: ✓ logged in all code paths

### Bugs Fixed:
1. **dailyLoginActions.ts** — claimDailyLogin() was reading/writing legacy `coins` field → fixed to `walletCoins`
2. **streakActions.ts** — streak milestone reward was updating legacy `coins` field → fixed to `walletCoins`
3. **lootDropActions.ts** — openLootDrop() was awarding to legacy `coins` field → fixed to `walletCoins`

### Build: ✓ Zero errors

---

## FA-002 — Ribilanciamento curva XP e reward per azione
**Status:** PASSED ✓
**Date:** 2026-04-08

### Changes:
1. **calculateTargetXp** — Old: `100 * level^1.5` (too steep: Level 10=3162 XP). New: `82 + 17*level + level^2`
   - Level 1: 100 | Level 10: 352 | Level 20: 822 | Level 30: 1492 | Level 40: 2362
   - Range 1-10: 100-352 XP (fast growth) ✓
   - Range 10-20: 352-822 XP (medium) ✓
   - Range 20-30: 822-1492 XP (slow) ✓
   - Range 30+: 1492+ XP (very slow) ✓
2. **ACTIONS rebalanced** — All actions now give both XP AND coins:
   - FISSATO: 10xp/0c → 15xp/10c
   - CONFERMATO: 15xp/5c → 20xp/8c
   - PRESENZIATO: 50xp/0c → 30xp/15c
   - CHIUSO: 200xp/50c → 50xp/25c
   - BONUS_SETTIMANALE and DEAL_CHIUSO unchanged (still used by venditori/sprint)

### Build: ✓ Zero errors

---

## FA-003 — Design system gaming: palette calda premium su tutto il CRM
**Status:** PASSED ✓
**Date:** 2026-04-08

### Design Tokens Added to globals.css:
- **Gaming Premium Palette**: `--color-gaming-bg-deep` (#0D0B0E), `--color-gaming-bg` (#141118), `--color-gaming-bg-card` (#1A1620), etc.
- **Fire Accent Scale**: `--color-fire-400` (#FF8C42), `--color-fire-500` (#FF6B1A), `--color-fire-glow`
- **Gaming Gold/Amber**: `--color-gaming-gold` (#FFD700), `--color-gaming-amber` (#FFAB00)
- **Gaming Shadows**: `--shadow-gaming-card`, `--shadow-gaming-elevated`, `--shadow-gaming-glow-fire`, `--shadow-gaming-glow-gold`
- **Utility Classes**: `.card-gaming`, `.glass-gaming`, `.gaming-section`, `.bg-gradient-gaming-*`, `.text-gaming-*`, `.divider-gaming`

### Components Updated (7 total):
1. **Sidebar.tsx** — `bg-gradient-gaming-sidebar`, fire-accent logo, gaming borders
2. **Topbar.tsx** — Dark premium wallet with gold glow, fire notification badge
3. **QuestPanel.tsx** — Gaming bg, fire progress bars, gold claim button
4. **StreakCounter.tsx** — Gaming bg tokens, gaming borders
5. **ShopClient.tsx** — Full light→dark: gaming bg, dark rarity cards with warm glow borders
6. **ProfileClient.tsx** — Gaming hero banner, fire evolution trail, gaming XP bar
7. **LeaderboardClient.tsx** — Full light→dark: gaming tabs, dark list rows, fire accents

### Pipeline Protection: ✓ Pipeline stays white/light — only gamification UI is dark premium

### Build: ✓ Zero errors

---

## FA-004 — Redesign LeadCard pipeline con rarità per funnel
**Status:** PASSED ✓
**Date:** 2026-04-08
**Commit:** fd7d0e7

### Changes:
1. **Funnel Rarity System** — Added `FUNNEL_RARITY_MAP` and `RARITY_STYLES` config objects:
   - Gold: CORSO 10 ORE, JOB SIMULATOR — amber left border, golden hover glow, amber funnel badge
   - Silver: TELEGRAM, TELEGRAM-TK, GOOGLE — slate left border, silver hover glow, slate funnel badge
   - Bronze: ORG, DATABASE, SOCIAL — orange left border, warm hover glow, orange funnel badge
2. **Card Left Border** — 3px colored left border based on funnel rarity (both row and vertical layouts)
3. **Hover Glow** — Rarity-colored shadow glow on hover
4. **Funnel Badge** — Colored background, text, and border per rarity (replacing plain gray)
5. **Name Prominence** — Bumped to text-[15px] font-bold leading-tight

### Pipeline Logic: ✓ Zero changes to click handlers, outcomes, or quick actions
### Build: ✓ Zero errors

---

## FA-005 — Redesign Store con card premium e effetto rarità
**Status:** PASSED ✓
**Date:** 2026-04-08
**Commit:** 7a2dbaf

### Changes:
1. **Rarity tiers redesigned** — 4 distinct visual tiers based on cost:
   - Comune (< 300): bronze/copper metallic border, warm amber glow on hover
   - Raro (300-999): silver/slate metallic border, silver glow on hover
   - Epico (1000-2999): gold border with pulsing gold glow animation (`shop-card-epic`)
   - Leggendario (3000+): purple border with pulsing purple glow + holographic shimmer overlay (`shop-card-legendary`)
2. **Holographic shimmer** — CSS `shop-holographic-overlay` on legendary cards: animated gradient sweep (purple/pink/blue) across card surface
3. **3D tilt on hover** — CSS perspective transform: cards subtly rotate on Y/X axis and lift on hover via `shop-card-3d` + `shop-card-inner`
4. **Animated Fenice Coin** — `shop-coin-animated` class: gentle Y-axis rotation on coin icons in both header wallet and card prices
5. **Sparkle effects** — Animated sparkle icons on epic+ cards in preview area with staggered timing
6. **Filter buttons upgraded** — Each category filter has distinct active gradient + glow matching its color theme; inactive filters have subtle hover transitions
7. **Rarity badges with icons** — Each rarity badge now shows a contextual icon (Flame/Sparkles/Crown/Gem)
8. **SafeWrapper** — Entire ShopClient wrapped in SafeWrapper error boundary
9. **CSS keyframes added** — `shop-holographic`, `shop-coin-rotate`, `shop-legendary-pulse`, `shop-epic-pulse`, `shop-sparkle`

### Store Logic: ✓ Zero changes to buy/equip/unequip logic, wallet reads walletCoins
### Build: ✓ Zero errors

---

## FA-006 — Redesign Profilo come Character Sheet RPG
**Status:** PASSED ✓
**Date:** 2026-04-08
**Commit:** ce2e84b

### Changes:
1. **CSS Fire Particles in Hero Banner** — 12 fire particles + 8 ember particles with `rpg-fire-particle` and `rpg-ember-float` keyframes. Staggered delays, variable drift direction. Particles rise from bottom of hero banner.
2. **AchievementShowcase → Dark Gaming Theme** — Full rewrite to dark gaming palette. Locked badges: grey silhouette with `badge-gaming-locked` class (opacity 0.45, grayscale 60%). Unlocked badges: luminous with `badge-gaming-unlocked` animation (pulsing glow). Tier-specific styling: bronze/amber, silver/grey, gold. Category headers with contextual icons.
3. **Quest Log → Dark Gaming Theme** — Converted from light white/ash to `gaming-bg-card`/`gaming-bg-surface`. Progress bars use `fire-500`/`gaming-gold`. Type badges (D/S) in dark blue/purple variants.
4. **Streak Section Enhanced** — Animated flame icon with `rpg-flame-flicker` + drop-shadow filter. Flame scales visually for 3/7/14 day tiers. Added milestone indicators (3g, 7g, 14g) with colored badges. Warm glow behind counter for active streaks.
5. **TitleSelector → Dark Gaming Theme** — Full conversion from purple-light to purple-dark palette. Locked titles at 45% opacity. Active title with ring glow.
6. **Settings Section → Dark Gaming Card** — Wrapped in gaming card with border and shadow.
7. **Prossimi Traguardi → Dark Gaming Theme** — Gold accent on dark background. Level badges with golden glow shadow.
8. **TL/Manager Profile View** — New dynamic route `/profilo/[userId]` with role-based access (MANAGER/ADMIN/TEAM_LEADER only). Back link to Monitor RPG. Eye icon button added to RPG Monitor table rows.
9. **SafeWrapper** — Entire ProfileClient wrapped in SafeWrapper error boundary.

### New CSS Keyframes:
- `rpg-fire-particle`: rising particle with drift + fade
- `rpg-ember-float`: smaller ember with drift
- `rpg-badge-unlock-glow`: pulsing glow for unlocked badges
- `rpg-quest-progress-shimmer`: progress bar shimmer

### Pipeline Logic: ✓ Not touched
### Build: ✓ Zero errors

---

## FA-007 — Redesign Classifica con podio animato e effetti ranking
**Status:** PASSED ✓
**Date:** 2026-04-08
**Commit:** ee766ca

### Changes:

#### CSS Keyframes Added/Enhanced (globals.css):
1. **lb-podium-gold/silver/bronze** — Enhanced with multi-layer glow and pulsating border-color
2. **lb-pedestal-gold-shimmer / lb-pedestal-silver-shimmer** — Horizontal gradient sweep across pedestals (metallic shimmer)
3. **lb-crown-float** — Floating crown animation (3px vertical oscillation) for 1st place
4. **lb-arrow-bounce-up** — Elastic bounce-up for green rank change arrow (spring cubic-bezier)
5. **lb-arrow-bounce-down** — Smooth bounce-down for red rank change arrow
6. **lb-row-slide-up / lb-row-slide-down** — Row slides up/down 8px when rank changes
7. **lb-score-pulse** — Score scales up 8% momentarily on rank improvement
8. **lb-podium-entrance** — Podium items scale+translate into view with spring bounce (staggered delays)

#### Component Updates (LeaderboardClient.tsx):
1. **Metallic Pedestals** — True metallic gradients: Gold (#ffd700→#a07d08), Silver (#c0cedc→#788898), Bronze (#cd7f32→#8b5722) with shimmer overlay
2. **Podium Background** — Subtle golden gradient glow behind podium area
3. **Gold Avatar Glow** — Background blur ring behind 1st place avatar
4. **Floating Crown** — Crown floats with lb-crown-float + gold drop-shadow
5. **Enhanced Rank Arrows** — ChevronUp/ChevronDown (strokeWidth=3), podium vs row variants, green glow on podium
6. **Row Slide Animations** — Rows slide up/down on rank change with highlight overlay
7. **Score Pulse** — Score pulses on rank improvement
8. **Self-Highlight** — brand-orange rank#, score, fire gradient left border with glow
9. **SafeWrapper** — Entire LeaderboardClient wrapped in SafeWrapper
10. **formatScore** — Used consistently for all score displays

### Pipeline Logic: ✓ Not touched
### Existing Tabs: ✓ GDO, Conferme, Venditori tabs maintained
### Build: ✓ Zero errors

---

## FA-008 — Scrigno a tempo: reward anticipato dopo X chiamate
**Status:** PASSED ✓
**Date:** 2026-04-09

### New Files Created:
1. **src/app/actions/timedChestActions.ts** — Server action `claimTimedChestReward(userId)`:
   - Rolls weighted rarity: 60% common (10-30c), 25% rare (50-100c), 12% epic (200c), 3% legendary (500c+title)
   - Applies streak multiplier + seasonal event multiplier
   - Updates `users.walletCoins` (not legacy `coins`), logs `coinTransactions`
   - Auto-equips legendary title if user has no active title

2. **src/components/TimedChest.tsx** — Client component with full lifecycle:
   - localStorage state tracking: action count, random threshold (5-15), spawn timestamp, ready timestamp, phase, daily reset
   - Listens to `reward_earned` custom events to count productive actions
   - Phases: hidden → spawned (countdown) → ready (openable) → opening (3s suspense) → revealed
   - Countdown: Random 15-30 minutes, displayed as MM:SS with progress bar
   - Minimizable floating card in bottom-right corner
   - Opening: 3-phase CSS suspense (growing shake → glow ring → particle escape → burst → reveal)
   - Celebrations: triggerCelebration for epic/legendary/rare
   - SafeWrapper in page.tsx

### CSS Keyframes Added (globals.css):
- `chest-entrance`, `chest-glow-pulse`, `chest-shake-subtle`, `chest-ready-pulse`
- `chest-sparkle`, `chest-opening-shake` (3-phase growing shake over 3s)
- `chest-glow-ring`, `chest-glow-intensify`, `chest-particle-escape` (6 variants)
- `chest-reveal-entrance`, `chest-reward-pulse`

### Data Persistence:
- localStorage (`crm-fenice-timed-chest`): chest state with daily reset (Europe/Rome)
- Server: reward granting only (walletCoins + coinTransaction)
- Multi-cycle: resets with new random threshold after each claim

### Pipeline Logic: ✓ Not touched
### Build: ✓ Zero errors

---

## FA-009 — Hot Streak visuale: la UI si infuoca dopo azioni consecutive
**Status:** PASSED ✓
**Date:** 2026-04-09

### New File Created:
1. **src/components/HotStreak.tsx** — Client component with full hot streak lifecycle:
   - Listens to `reward_earned` custom events (same event system as TimedChest)
   - Tracks action timestamps in a ref, filters to 10-minute window
   - Three modes: `inactive` (no visual), `active` (3+ actions), `intense` (5+ actions)
   - Active mode: amber/orange fire gradient border (top + sides), subtle glow shadow, pulsing flame badge
   - Intense mode: red/amber fire gradient border (faster animation), stronger glow, larger badge with "ON FIRE" text
   - Auto-deactivates after 5 minutes without any action (decay timer)
   - Respects `getAnimationsEnabled()` toggle
   - When inactive, renders children with zero overhead (no wrapper div)

### CSS Keyframes Added (globals.css):
- `hot-streak-fire-sweep` — gradient position sweep along borders (2s, normal mode)
- `hot-streak-fire-sweep-intense` — faster sweep with brightness filter (1.2s, intense mode)
- `hot-streak-badge-pulse` — flame badge scale pulse (1.5s)
- `hot-streak-badge-pulse-intense` — bigger pulse with drop-shadow throb (0.8s)

### Pages Updated:
1. **src/app/(dashboard)/page.tsx** — PipelineBoard wrapped in `<SafeWrapper><HotStreak>...</HotStreak></SafeWrapper>`
2. **src/app/(dashboard)/conferme/page.tsx** — ConfermeBoard wrapped in `<SafeWrapper><HotStreak>...</HotStreak></SafeWrapper>`

### Pipeline Logic: ✓ Not touched — purely visual wrapper, zero changes to click/outcome/action logic
### Build: ✓ Zero errors

---

## FA-010 — Notifiche FOMO live: toast quando un collega agisce
**Status:** PASSED ✓
**Date:** 2026-04-09

### Architecture:
- **No new Supabase channels** — all 3 event sources added to the existing `public:leads-changes` channel via additional `.on()` handlers:
  1. `postgres_changes` on `leadEvents` INSERT → dispatches `fomo_lead_event` CustomEvent
  2. `postgres_changes` on `userAchievements` INSERT → dispatches `fomo_achievement_event` CustomEvent
  3. `broadcast` on `fomo_hotstreak` → dispatches `fomo_hotstreak_event` CustomEvent
- **RealtimeProvider context** — exposes `broadcastFomo(event, payload)` via `useRealtimeBroadcast()` hook for HotStreak to broadcast streak activation to colleagues

### New File Created:
1. **src/components/FomoToast.tsx** — Client component:
   - Caches GDO/CONFERME user names from `getGdoAndConfermeUsers()` on mount
   - Listens to 3 custom events: `fomo_lead_event`, `fomo_achievement_event`, `fomo_hotstreak_event`
   - Filters: own events excluded, only GDO/CONFERME role events shown
   - Event type mapping: `appointment_set` → "ha fissato un appuntamento!", `conferme_outcome_set`/`confirmed` → "ha confermato un appuntamento!", `conferme_recall_scheduled` → "ha fissato un richiamo!", achievements → "ha sbloccato un badge!", hotstreak → "è in hot streak!"
   - Max 2 toasts visible, 4s auto-dismiss, oldest evicted when full
   - Slide-in from right with CSS transition (300ms ease-out)
   - Contextual icons: Phone (appointments), CheckCircle (confirmations), Trophy (achievements), Flame (hot streak)
   - Contextual colors: brand-orange (appointments), emerald (confirmations), gold (achievements), fire (hot streak)
   - Gaming dark card style with glow shadow matching toast type
   - Shimmer accent line on top with `fomo-shimmer` CSS animation
   - Only renders for GDO/CONFERME roles
   - Click to dismiss

### Files Modified:
1. **RealtimeProvider.tsx** — Added `leadEvents` INSERT, `userAchievements` INSERT, and `broadcast fomo_hotstreak` listeners to existing channel. Added context with `broadcastFomo` function.
2. **HotStreak.tsx** — When hot streak activates (3+ actions), broadcasts `fomo_hotstreak` via `useRealtimeBroadcast()`. Resets broadcast flag on decay.
3. **layout.tsx** — Added `<SafeWrapper><FomoToast /></SafeWrapper>` inside RealtimeProvider.
4. **globals.css** — Added `fomo-shimmer` keyframe animation.

### Silent Fail: ✓ Every handler wrapped in try/catch, Realtime failure = no toasts (graceful degradation)
### Pipeline Logic: ✓ Not touched
### Build: ✓ Zero errors

---

## FA-011 — Progress nudge: micro-banner "Mancano solo X!" vicino ai traguardi
**Status:** PASSED ✓
**Date:** 2026-04-09

### Changes:

#### 1. Quest Proximity Nudge (QuestPanel.tsx — QuestCard)
- When quest progress >= 70% and not yet completed, a golden micro-banner appears below the progress bar
- Text: "Solo X azioni al premio!" with Zap icon
- Uses `nudge-pulse` CSS animation (gentle opacity pulse)
- Correctly calculates remaining actions: `targetValue - currentValue`

#### 2. Level Proximity Nudge (new: LevelNudge.tsx)
- New lightweight component that fetches user level/XP via `getUserLevelProgress()` server action
- When XP progress >= 80% of level target, shows a pulsing golden banner
- Text: "Quasi al livello X!" + "Mancano solo Y XP — continua cosi!"
- Mini XP progress bar on the right showing exact XP/target
- Refreshes on `reward_earned` events to update in real-time
- Uses `level-nudge-pulse` CSS animation (scale + golden glow pulse)
- Added to GDO dashboard (page.tsx) and Conferme dashboard (conferme/page.tsx)
- Wrapped in SafeWrapper on both pages

#### 3. Streak Inline Nudge (StreakCounter.tsx)
- When streak > 0 and not active today (but before at-risk hour >= 18), shows inline amber pill badge
- Text: "Non perdere la streak!" with AlertTriangle icon
- Only shows in `default` visual state (not when already `safe` or `at-risk`)
- Complements the existing StreakAnxietyBanner which handles >= 18:00 Rome time
- Uses `nudge-pulse` CSS animation

#### 4. Server Action (sprintActions.ts)
- New `getUserLevelProgress(userId)` returns: level, experience, targetXp, progressPercent, remainingXp

#### CSS Keyframes Added (globals.css):
- `nudge-pulse` — gentle opacity pulse (0.85 → 1 → 0.85) for quest and streak nudges
- `level-nudge-pulse` — scale (1 → 1.02) + golden box-shadow pulse for level nudge

### Pipeline Logic: ✓ Not touched
### Build: ✓ Zero errors

---

## FA-012 — Animazione apertura scrigno con suspense
**Status:** PASSED ✓
**Date:** 2026-04-09

### New File Created:
1. **src/components/ChestOpeningAnimation.tsx** — Shared reusable 3-phase suspense animation component:
   - Phase 1 (0-1s): Crescendo shake (co-shake-phase1) — gentle tremor building to medium intensity
   - Phase 2 (1-2s): Intense shake + scale + glow ring + escaping sparkle particles (co-shake-phase2)
   - Phase 3 (2-3s): Burst scale out + particle explosion + rarity text reveal + confetti (legendary)
   - Rarity-specific visuals:
     - Common: stone/grey tones, 8 burst particles, muted glow
     - Rare: blue tones, 16 burst particles, blue glow
     - Epic: purple tones, 24 burst particles, intense purple glow
     - Legendary: gold tones, 32 burst particles + 12 confetti rain, intense gold glow
   - Plays drum roll during phases 1-2, rarity-specific reveal sound at phase 3
   - Respects `getAnimationsEnabled()` — skips animation if disabled
   - Renders children (reward content) after animation completes

### Sound Engine Updates (soundEngine.ts):
- **chest_drum_roll**: 24-hit crescendo with accelerating rhythm over 2s + final snare hit
- **chest_reveal_common**: Muted sine thud (400→300Hz, 0.4s)
- **chest_reveal_rare**: Bright 2-note chime C5+G5 (0.5s)
- **chest_reveal_epic**: Rising sawtooth sweep + C5 major chord stab (0.8s)
- **chest_reveal_legendary**: Impact whoosh + ascending arpeggio C5→E5→G5→C6 + sustained golden chord (1.5s)

### CSS Keyframes Added (globals.css):
- `co-shake-phase1`: 20-step crescendo shake (1px→5px translation, 0.3°→2° rotation)
- `co-shake-phase2`: 25-step intense shake (5px→12px, scale 1→1.15)
- `co-burst-scale`: Scale up then collapse to 0
- `co-glow-pulse`: Radial glow opacity + scale pulse
- `co-ring-expand`: Border ring expanding outward
- `co-icon-wobble`: Package icon wobble during phase2
- `co-particle-escape`: Sparkles escaping outward (phase2)
- `co-particle-burst`: Particles exploding outward (phase3)
- `co-flash`: Full-screen white flash
- `co-confetti-fall`: Golden confetti raining down (legendary only)
- `co-text-fade`, `co-text-reveal`: Phase text animations

### Components Updated:
1. **TimedChest.tsx** — Opening phase replaced with ChestOpeningAnimation. Server call happens first to get rarity, then animation plays with correct visuals. `handleAnimationComplete` transitions to revealed phase with celebration effects.
2. **LootDropModal.tsx** — Same pattern: server call first, then ChestOpeningAnimation with known rarity. Removed old shaking/opening phases. Cleaned up unused 'shaking' phase type.

### Reuse: ✓ Both TimedChest and LootDropModal use shared ChestOpeningAnimation
### All CSS-only: ✓ No external animation libraries
### Pipeline Logic: ✓ Not touched
### Build: ✓ Zero errors

---

## FA-013 — Daily bonus crescente con calendar visuale
**Status:** PASSED ✓
**Date:** 2026-04-09

### Server Changes (dailyLoginActions.ts):
1. **Escalating weekly rewards** — Mon 5c, Tue 8c, Wed 12c, Thu 15c, Fri 20c, Sat 30c, Sun 50c
2. **Weekly calendar data** — `checkDailyLoginStatus` now returns `calendar: DayCalendarEntry[]` with 7 days, each showing label/date/reward/status
3. **Sunday streak bonus** — If all Mon-Sat claimed, Sunday awards 50 coins + "Maratoneta Settimanale" title. If not all claimed, Sunday gives 15 coins base
4. **Auto-equip title** — Weekly bonus title auto-equipped if user has no active title
5. **Day-of-week reward calculation** — `claimDailyLogin` uses `getTodayWeekIndex()` to determine escalating reward instead of flat streak-tier bonus
6. **New helpers** — `getWeekMondayStr()`, `getTodayWeekIndex()` for Rome-timezone week calculations
7. **Exported type** — `DayCalendarEntry` interface for client import

### Component Redesign (DailyLoginReward.tsx):
1. **7-day calendar strip** — Horizontal row of 7 day boxes (Mon-Sun) with:
   - Day label (Lun, Mar, Mer, etc.)
   - Status icon: Check (claimed/green), X (missed/red), Gift (today/gold), Lock (future/dimmed)
   - Coin amount per day
   - Sunday crown indicator (gold if eligible, grey if not)
2. **Today highlighted** — Golden glow animation (`daily-calendar-glow`), gradient background, gold accents
3. **Claimed days** — Green border + emerald check icon
4. **Missed days** — Red border + dimmed opacity + X icon
5. **Future days** — Very dimmed with lock icon
6. **Claimed phase** — Mini calendar summary strip showing week status after claiming
7. **Weekly bonus title banner** — Purple banner announcing "Maratoneta Settimanale" title unlock
8. **Coin cascade** — Existing coin particle animation using Coins icon (replaced emoji)
9. **Compact header** — Flame icon + streak badge + welcome text in horizontal layout
10. **Dark gaming card** — Full dark premium style consistent with gamification UI
11. **Extracted CalendarDay component** — Clean separation for each day box rendering

### CSS Keyframe Added (globals.css):
- `daily-calendar-glow` — Pulsing golden box-shadow + inset glow for today's calendar box

### Pipeline Logic: ✓ Not touched
### Build: ✓ Zero errors

---

## FA-014 — Celebrazione level-up epica con evoluzione Fenice
**Status:** PASSED ✓
**Date:** 2026-04-09

### Architecture:
- CelebrationOverlay now listens DIRECTLY to `reward_earned` events for level-up detection with evolution awareness
- Detects evolution by comparing old/new stage boundaries (client-side mirror of EVOLUTION_STAGES)
- Triggers `evolution_fanfare` sound for evolutions, `level_up` for regular level-ups
- CelebrationDetail extended with: `isEvolution`, `previousStageName`, `newStageName`, `previousStageEmoji`, `newStageEmoji`

### Level-Up Phases (Regular — 5s total):
1. **fire_intro** (0-1.2s): Dark overlay + 20 CSS fire particles + 12 embers rising from bottom
2. **level_text** (1.2s): Typewriter "LIVELLO X!" with golden gradient text + radial burst
3. **confetti_finale** (2.8s): Confetti cascade (30 particles) + fire + can dismiss
4. Auto-dismiss at 5s

### Level-Up Phases (Evolution — 8s total):
1. **fire_intro** (0-1.2s): Dark overlay + fire particles + old stage emoji entrance
2. **level_text** (1.2s): Typewriter "LIVELLO X!" with golden gradient
3. **evolution_old** (2.5s): Old stage emoji dissolves (brightness burn + shrink + blur)
4. **evolution_flash** (3.5s): Flash burst + 3 expanding amber rings
5. **evolution_new** (4.0s): New stage materializes (scale 3→1) + golden orbiting particles
6. **confetti_finale** (5.5s): "Evoluzione Fenice" title reveal + confetti cascade

### Evolution Stage Boundaries:
- Uovo (1-7) → Pulcino (8-17) → Fenice Giovane (18-25) → Fenice di Fuoco (26-34) → Divinita Fenice (35+)

### New Sound — evolution_fanfare:
- Low rumble → 7-note ascending arpeggio C4→C6 → sustained power chord → high shimmer tail (3s total)

### CSS Keyframes Added (12):
- `celeb-fire-rise`, `celeb-fire-rise-slow`, `celeb-ember-drift`, `celeb-level-entrance`, `celeb-level-glow`
- `celeb-evo-old-stage`, `celeb-evo-flash`, `celeb-evo-new-stage`, `celeb-evo-ring`, `celeb-evo-name-reveal`
- `celeb-confetti-cascade`, `celeb-radial-burst`, `celeb-dismiss-hint`

### Files Modified:
1. **CelebrationOverlay.tsx** — Full redesign with phased level-up sequence + evolution detection
2. **animationUtils.ts** — CelebrationDetail extended with evolution fields
3. **soundEngine.ts** — New `evolution_fanfare` SoundType + playEvolutionFanfare()
4. **globals.css** — 12 new keyframes for epic level-up effects
5. **RewardFeedProvider.tsx** — Removed level-up trigger (now handled by CelebrationOverlay directly)

### SafeWrapper: ✓ Already wrapped on page.tsx
### Pipeline Logic: ✓ Not touched
### Build: ✓ Zero errors

---
